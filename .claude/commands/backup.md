CLAUDE.md의 **백업해줘** 규칙을 실행합니다.

1. `/root/dogam/backup/` 안의 폴더 목록을 확인해 가장 최근 백업 폴더를 찾는다.
2. 백업 폴더가 없으면 "백업 폴더가 없어요. /backup-new 로 새 백업을 먼저 만들어주세요." 라고 안내하고 중단한다.
3. 찾은 폴더에 `/root/dogam/` 파일 전체를 덮어쓴다. 단, `/root/dogam/backup/` 폴더는 제외한다.
4. 완료 후 덮어쓴 폴더명을 보고한다.
